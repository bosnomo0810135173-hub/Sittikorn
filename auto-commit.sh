#!/bin/bash
# auto-commit.sh - Script สำหรับ auto-commit

# สี
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo -e "${YELLOW}🔄 กำลังตรวจสอบการเปลี่ยนแปลง...${NC}"

# เช็คว่ามีไฟล์เปลี่ยนแปลงหรือไม่
if [ -n "$(git status --porcelain)" ]; then
    echo -e "${GREEN}📝 พบการเปลี่ยนแปลง! กำลัง commit...${NC}"
    
    # เพิ่มไฟล์ทั้งหมด
    git add .
    
    # Commit ด้วยวันที่และเวลาปัจจุบัน
    git commit -m "Auto commit: $(date +'%Y-%m-%d %H:%M:%S')"
    
    echo -e "${GREEN}✅ Commit สำเร็จ!${NC}"
    
    # ถ้าต้องการ auto-push ด้วย ให้เอา # ออก
    # echo -e "${YELLOW}📤 กำลัง push...${NC}"
    # git push
    # echo -e "${GREEN}✅ Push สำเร็จ!${NC}"
else
    echo -e "${YELLOW}ℹ️  ไม่มีการเปลี่ยนแปลง${NC}"
fi
